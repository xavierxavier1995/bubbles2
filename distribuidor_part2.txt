const MultiStepForm = ({ isOpen, onClose }: { isOpen: boolean, onClose: () => void }) => {
  const [step, setStep] = useState(1);
  const [formData, setFormData] = useState({
    name: '',
    email: '',
    phone: '',
    city: '',
    state: '',
    experience: '',
    investment: ''
  });

  if (!isOpen) return null;

  const nextStep = () => setStep(prev => prev + 1);
  const prevStep = () => setStep(prev => prev - 1);

  return (
    <div className="fixed inset-0 z-[100] flex items-center justify-center p-4 bg-black/90 backdrop-blur-md">
      <motion.div 
        initial={{ y: 50, opacity: 0 }}
        animate={{ y: 0, opacity: 1 }}
        className="bg-[#121212] border border-white/10 rounded-[40px] p-8 md:p-12 max-w-2xl w-full relative overflow-hidden shadow-2xl"
      >
        <button onClick={onClose} className="absolute top-8 right-8 text-white/40 hover:text-white transition-colors z-20">
          <X size={24} />
        </button>
        
        <div className="absolute top-0 left-0 w-full h-2 bg-white/5">
          <motion.div 
            className="h-full bg-[#F4CDD4]" 
            initial={{ width: 0 }}
            animate={{ width: `${(step / 3) * 100}%` }}
          />
        </div>

        <div className="relative z-10">
          <div className="mb-10">
            <span className="text-[#F4CDD4] text-[10px] font-black uppercase tracking-[0.3em] mb-2 block">Passo {step} de 3</span>
            <h2 className="text-2xl md:text-3xl font-black text-white tracking-tight">
              {step === 1 && "Vamos começar pelo básico"}
              {step === 2 && "Onde você pretende atuar?"}
              {step === 3 && "Seu perfil de investidor"}
            </h2>
          </div>

          <form className="space-y-6">
            {step === 1 && (
              <motion.div initial={{ opacity: 0, x: 20 }} animate={{ opacity: 1, x: 0 }} className="space-y-4">
                <div className="space-y-2">
                  <label className="text-white/40 text-[10px] font-black uppercase tracking-widest">Nome Completo</label>
                  <input 
                    type="text" 
                    placeholder="Seu nome aqui"
                    className="w-full bg-white/5 border border-white/10 rounded-2xl p-4 text-white focus:border-[#F4CDD4] outline-none transition-colors"
                    value={formData.name}
                    onChange={e => setFormData({...formData, name: e.target.value})}
                  />
                </div>
                <div className="space-y-2">
                  <label className="text-white/40 text-[10px] font-black uppercase tracking-widest">E-mail Corporativo</label>
                  <input 
                    type="email" 
                    placeholder="seu@email.com"
                    className="w-full bg-white/5 border border-white/10 rounded-2xl p-4 text-white focus:border-[#F4CDD4] outline-none transition-colors"
                    value={formData.email}
                    onChange={e => setFormData({...formData, email: e.target.value})}
                  />
                </div>
                <div className="space-y-2">
                  <label className="text-white/40 text-[10px] font-black uppercase tracking-widest">WhatsApp</label>
                  <input 
                    type="tel" 
                    placeholder="(00) 00000-0000"
                    className="w-full bg-white/5 border border-white/10 rounded-2xl p-4 text-white focus:border-[#F4CDD4] outline-none transition-colors"
                    value={formData.phone}
                    onChange={e => setFormData({...formData, phone: e.target.value})}
                  />
                </div>
              </motion.div>
            )}

            {step === 2 && (
              <motion.div initial={{ opacity: 0, x: 20 }} animate={{ opacity: 1, x: 0 }} className="space-y-4">
                <div className="grid grid-cols-2 gap-4">
                  <div className="space-y-2">
                    <label className="text-white/40 text-[10px] font-black uppercase tracking-widest">Estado</label>
                    <select 
                      className="w-full bg-white/5 border border-white/10 rounded-2xl p-4 text-white focus:border-[#F4CDD4] outline-none transition-colors appearance-none"
                      value={formData.state}
                      onChange={e => setFormData({...formData, state: e.target.value})}
                    >
                      <option value="">Selecione</option>
                      <option value="SP">São Paulo</option>
                      <option value="RJ">Rio de Janeiro</option>
                      <option value="MG">Minas Gerais</option>
                      <option value="PR">Paraná</option>
                      <option value="SC">Santa Catarina</option>
                      <option value="RS">Rio Grande do Sul</option>
                    </select>
                  </div>
                  <div className="space-y-2">
                    <label className="text-white/40 text-[10px] font-black uppercase tracking-widest">Cidade</label>
                    <input 
                      type="text" 
                      placeholder="Sua cidade"
                      className="w-full bg-white/5 border border-white/10 rounded-2xl p-4 text-white focus:border-[#F4CDD4] outline-none transition-colors"
                      value={formData.city}
                      onChange={e => setFormData({...formData, city: e.target.value})}
                    />
                  </div>
                </div>
                <div className="space-y-2">
                  <label className="text-white/40 text-[10px] font-black uppercase tracking-widest">Já atua no mercado pet?</label>
                  <div className="grid grid-cols-2 gap-4">
                    {['Sim', 'Não'].map(opt => (
                      <button
                        key={opt}
                        type="button"
                        onClick={() => setFormData({...formData, experience: opt})}
                        className={`p-4 rounded-2xl border transition-all font-bold text-sm ${
                          formData.experience === opt 
                          ? "bg-[#F4CDD4] text-[#080808] border-[#F4CDD4]" 
                          : "bg-white/5 text-white border-white/10 hover:border-white/30"
                        }`}
                      >
                        {opt}
                      </button>
                    ))}
                  </div>
                </div>
              </motion.div>
            )}

            {step === 3 && (
              <motion.div initial={{ opacity: 0, x: 20 }} animate={{ opacity: 1, x: 0 }} className="space-y-4">
                <div className="space-y-2">
                  <label className="text-white/40 text-[10px] font-black uppercase tracking-widest">Capacidade de Investimento</label>
                  <div className="grid gap-3">
                    {[
                      'R$ 10k - R$ 30k',
                      'R$ 30k - R$ 60k',
                      'R$ 60k - R$ 100k',
                      'Acima de R$ 100k'
                    ].map(opt => (
                      <button
                        key={opt}
                        type="button"
                        onClick={() => setFormData({...formData, investment: opt})}
                        className={`p-4 rounded-2xl border transition-all font-bold text-left text-sm flex items-center justify-between ${
                          formData.investment === opt 
                          ? "bg-[#F4CDD4] text-[#080808] border-[#F4CDD4]" 
                          : "bg-white/5 text-white border-white/10 hover:border-white/30"
                        }`}
                      >
                        {opt}
                        {formData.investment === opt && <CheckCircle size={18} />}
                      </button>
                    ))}
                  </div>
                </div>
              </motion.div>
            )}

            <div className="flex gap-4 pt-6">
              {step > 1 && (
                <button 
                  type="button" 
                  onClick={prevStep}
                  className="flex-1 bg-white/5 text-white py-4 rounded-2xl font-black uppercase tracking-widest border border-white/10 hover:bg-white/10 transition-colors"
                >
                  Voltar
                </button>
              )}
              <button 
                type="button" 
                onClick={step === 3 ? onClose : nextStep}
                className="flex-[2] bg-[#F4CDD4] text-[#080808] py-4 rounded-xl font-black uppercase tracking-widest hover:bg-white transition-colors shadow-lg"
              >
                {step === 3 ? "Enviar Candidatura" : "Continuar"}
              </button>
            </div>
          </form>
        </div>
        
        <div className="absolute -bottom-32 -left-32 w-80 h-80 bg-[#F4CDD4]/5 blur-[100px] rounded-full pointer-events-none" />
      </motion.div>
    </div>
  );
};

// --- Main Page ---

export default function DistribuidorGabriel() {
  const [isFormOpen, setIsFormOpen] = useState(false);
  const [isExitPopupOpen, setIsExitPopupOpen] = useState(false);
  const [activeCommunityTab, setActiveCommunityTab] = useState(0);
  const heroButtonRef = useRef<HTMLButtonElement>(null);
  const statsCarouselRef = useRef<HTMLDivElement>(null);
  const profitabilityCarouselRef = useRef<HTMLDivElement>(null);
  const testimonialsCarouselRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    const handleMouseLeave = (e: MouseEvent) => {
      if (e.clientY <= 0 && !import.meta.env.DEV) {
        setIsExitPopupOpen(true);
      }
    };
    document.addEventListener('mouseleave', handleMouseLeave);
    return () => document.removeEventListener('mouseleave', handleMouseLeave);
  }, []);

  const communityItems = [
    { title: "Elite Bubbles", icon: Award, desc: "Acesso exclusivo aos lançamentos e treinamentos com os maiores groomers do Brasil." },
    { title: "Network", icon: Users, desc: "Conexão direta com outros distribuidores de sucesso para troca de experiências e estratégias." },
    { title: "Educação", icon: GraduationCap, desc: "Plataforma de ensino contínuo para sua equipe de vendas e para seus clientes groomers." },
    { title: "Eventos", icon: Globe, desc: "Presença garantida e suporte em feiras regionais e nacionais do setor pet." }
  ];

  return (
    <div className="min-h-screen bg-[#080808] selection:bg-[#F4CDD4] selection:text-[#080808] font-sans overflow-x-hidden">
      {/* Background Effects */}
      <div className="fixed inset-0 pointer-events-none z-0">
        <div className="absolute top-0 left-1/4 w-[500px] h-[500px] bg-[#F4CDD4]/5 blur-[150px] rounded-full" />
        <div className="absolute bottom-0 right-1/4 w-[600px] h-[600px] bg-[#F4CDD4]/5 blur-[180px] rounded-full" />
        <div className="absolute inset-0 bg-[radial-gradient(circle_at_center,_#ffffff05_1px,_transparent_1px)] bg-[size:40px_40px]" />
      </div>

      {/* Header */}
      <header className="relative z-50 py-8 px-6 md:px-10 flex justify-between items-center max-w-7xl mx-auto">
        <Logo />
        <div className="flex items-center gap-4 md:gap-8">
          <a href="#rentabilidade" className="text-white/40 hover:text-white text-[10px] font-black uppercase tracking-widest transition-colors hidden md:block">Rentabilidade</a>
          <a href="#suporte" className="text-white/40 hover:text-white text-[10px] font-black uppercase tracking-widest transition-colors hidden md:block">Suporte</a>
          <motion.button 
            whileHover={{ scale: 1.05 }}
            whileTap={{ scale: 0.95 }}
            onClick={() => setIsFormOpen(true)}
            className="bg-white/5 hover:bg-white/10 text-white border border-white/10 px-6 py-2.5 rounded-full text-[10px] font-black uppercase tracking-widest transition-all"
          >
            Seja Distribuidor
          </motion.button>
        </div>
      </header>

      {/* Hero Section */}
      <section className="relative z-10 pt-12 md:pt-20 pb-16 md:pb-32 px-6 md:px-10 max-w-7xl mx-auto">
        <div className="grid lg:grid-cols-2 gap-12 md:gap-20 items-center">
          <motion.div
            initial={{ opacity: 0, x: -50 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ duration: 0.8, ease: "easeOut" }}
          >
            <div className="inline-flex items-center gap-2 px-4 py-2 bg-white/5 border border-white/10 rounded-full mb-8 shadow-xl">
              <span className="w-2 h-2 bg-[#F4CDD4] rounded-full animate-pulse shadow-[0_0_10px_#F4CDD4]" />
              <span className="text-white/60 text-[10px] font-black uppercase tracking-widest">Oportunidade de Expansão 2026</span>
            </div>
            
            <h1 className="text-4xl sm:text-5xl md:text-7xl font-black text-white mb-8 leading-[0.95] tracking-tighter">
              A Marca que os <br />
              <span className="text-[#F4CDD4] drop-shadow-[0_0_30px_rgba(244,205,212,0.3)]">Groomers Amam,</span> <br />
              O Negócio que você <br />
              <span className="italic font-serif font-light">Deseja.</span>
            </h1>
            
            <p className="text-white/60 text-lg md:text-xl mb-12 leading-relaxed max-w-xl font-medium">
              Seja o parceiro estratégico da Bubbles® na sua região. 
              Produtos de <span className="text-white font-bold">alta performance</span>, margens agressivas e uma comunidade que não para de crescer.
            </p>
            
            <div className="flex flex-col sm:flex-row gap-6">
              <motion.button 
                ref={heroButtonRef}
                whileHover={{ 
                  scale: 1.02, 
                  boxShadow: '0 0 40px rgba(244,205,212,0.4)',
                  y: -2
                }}
                whileTap={{ scale: 0.98 }}
                onClick={() => setIsFormOpen(true)}
                className="bg-[#F4CDD4] text-[#080808] px-10 md:px-12 py-4 md:py-5 rounded-xl md:rounded-2xl font-black uppercase tracking-widest text-xs md:text-sm transition-all flex items-center justify-center gap-3 group shadow-2xl"
              >
                Quero ser um Distribuidor <ArrowRight size={18} className="group-hover:translate-x-1 transition-transform" />
              </motion.button>
              
              <div className="flex -space-x-4 items-center">
                {[1, 2, 3, 4].map((i) => (
                  <div key={i} className="w-10 h-10 md:w-12 md:h-12 rounded-full border-2 border-[#080808] overflow-hidden bg-[#121212]">
                    <img 
                      src={`https://randomuser.me/api/portraits/men/${i + 10}.jpg`} 
                      alt="User" 
                      className="w-full h-full object-cover grayscale hover:grayscale-0 transition-all duration-500"
                      referrerPolicy="no-referrer"
                    />
                  </div>
                ))}
                <div className="pl-8">
                  <p className="text-white font-black text-xs md:text-sm tracking-tight">+50 Parceiros</p>
                  <p className="text-white/40 text-[10px] font-bold uppercase tracking-widest">Em todo o Brasil</p>
                </div>
              </div>
            </div>
          </motion.div>
          
          <motion.div
            initial={{ opacity: 0, scale: 0.8 }}
            animate={{ opacity: 1, scale: 1 }}
            transition={{ duration: 1, ease: "easeOut" }}
            className="relative aspect-square lg:aspect-auto lg:h-[600px]"
          >
            <div className="absolute inset-0 bg-[#F4CDD4]/10 blur-[120px] rounded-full animate-pulse" />
            <HeroCarousel />
          </motion.div>
        </div>
      </section>

      <BenefitsMarquee />
